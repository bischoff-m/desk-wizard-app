{
  "name": "desk-wizard-monorepo",
  "version": "0.1.0",
  "description": "DeskWizard Monorepo",
  "license": "GPL-3.0",
  "private": true,
  "author": "Marco Bischoff <bischoff_m@aim.com>",
  "engines": {
    "node": ">=20.9.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "clean": "lerna clean"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.0"
  }
}
